# copy to ~/.gitconfig.local and fill in per machine. this file is NOT tracked.
#   cp git/.gitconfig.local.example ~/.gitconfig.local

[user]
	name = YOUR_NAME
	email = YOUR_EMAIL
	# signingkey = YOUR_KEY_ID

# uncomment to sign commits (needs the signingkey above)
# [commit]
# 	gpgsign = true
